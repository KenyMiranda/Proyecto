import { FilterHorarioPipe } from './filter-horario.pipe';

describe('FilterHorarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHorarioPipe();
    expect(pipe).toBeTruthy();
  });
});
