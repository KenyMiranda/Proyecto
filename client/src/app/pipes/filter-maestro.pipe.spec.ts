import { FilterMaestroPipe } from './filter-maestro.pipe';

describe('FilterMaestroPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMaestroPipe();
    expect(pipe).toBeTruthy();
  });
});
