import { FilterCalifPipe } from './filter-calif.pipe';

describe('FilterCalifPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCalifPipe();
    expect(pipe).toBeTruthy();
  });
});
